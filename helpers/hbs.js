const moment = require('moment')

module.exports = {
    compareStrings: function (str, str1) {
        if(str == str1){
            return true;
        }else{
            return false;
        }
      },
}